// level {3}

const getUsers = async (users) => {
    const newUsers = [];
    await users.map(async(user, index) => {
        newUser.id = await index;
        newUsers.push(newUser);
    })
  
    return newUsers;
  }
